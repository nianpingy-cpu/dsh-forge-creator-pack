{
  "name": "@dsh-forge-creator/plugin-creator-cover",
  "version": "0.1.0",
  "description": "Platform-ready cover generation: centralized platform profiles, deterministic local layout + mock background, optional ComfyUI external HTTP/API adapter (no vendoring)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

{
  "name": "@dsh-forge-creator/plugin-ffmpeg",
  "version": "0.1.0",
  "description": "FFmpeg adapter (carried in from @dsh-forge/plugin-ffmpeg, MIT) for the creator pack: probe, clip, transcode, concat, audio extract/convert, thumbnail, compress (workspace-write with overwrite guard)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

{
  "name": "@dsh-forge-creator/plugin-creator-transcribe",
  "version": "0.1.0",
  "description": "Creator-grade transcription: timed segments, SRT/VTT/ASS subtitles, chapters, language detection (Whisper-compatible provider + deterministic mock)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

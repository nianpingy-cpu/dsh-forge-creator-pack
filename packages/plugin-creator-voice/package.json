{
  "name": "@dsh-forge-creator/plugin-creator-voice",
  "version": "0.1.0",
  "description": "Licensed TTS / voice clone: authorized voice references (source/owner/checksum/createdAt, no biometrics), deterministic mock + OpenVoice-compatible external adapter (no vendoring)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

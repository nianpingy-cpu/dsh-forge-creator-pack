{
  "name": "@dsh-forge-creator/plugin-creator-motion",
  "version": "0.1.0",
  "description": "Templated, verifiable programmatic video rendering: local deterministic mock renderer + generic Remotion-compatible HTTP/command provider interface (Remotion license is custom/NOASSERTION — no vendoring)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

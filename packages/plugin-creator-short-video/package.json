{
  "name": "@dsh-forge-creator/plugin-creator-short-video",
  "version": "0.1.0",
  "description": "Topic/script -> short-video job adapter: plan -> generate -> status -> assets lifecycle with a deterministic mock and a MoneyPrinterTurbo-compatible provider adapter (no vendoring)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

{
  "name": "@dsh-forge-creator/plugin-creator-capture",
  "version": "0.1.0",
  "description": "Controlled, licensed media capture for creators: yt-dlp adapter with typed argv[], rights/provenance, workspace-bound writes (never an arbitrary downloader)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

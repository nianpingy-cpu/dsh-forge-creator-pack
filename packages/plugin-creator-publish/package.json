{
  "name": "@dsh-forge-creator/plugin-creator-publish",
  "version": "0.1.0",
  "description": "Controlled multi-platform publishing: draft -> validate -> preview -> explicit approval -> schedule/publish -> status, with idempotency keys, retry safety (request-failed vs status-unknown), dry-run, and capability discovery; mock + Postiz-compatible external adapter (AGPL-3.0, no vendoring)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge-creator/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}

{
  "name": "@dsh-forge-creator/core",
  "version": "0.1.0",
  "description": "Adapted @dsh-forge/core (MIT) + Creator-domain contracts: process runner, diagnostics, workspace policy, contract test kit, creator assets/rights/publish/credentials/policy",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
